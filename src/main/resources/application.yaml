
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/inventorymanager
    username: root
    password: example
  jpa:
    hibernate:
      ddl-auto: validate
